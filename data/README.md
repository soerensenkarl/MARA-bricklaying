# Data

`run_8.MP4` is a continuous video recording of run 8.

`placement_accuracy_still_frames/` contains top-down layer images used for placement accuracy analysis.

Filename pattern:

```text
run<run number>_layer<layer number>.png
```

The folder contains 40 PNG files: four layers for runs 1-10.
